# Engine BFF only — Node 22 ships with fetch.
FROM node:22-alpine
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm ci --omit=dev
COPY server/engine-bff.mjs ./server/engine-bff.mjs
ENV NODE_ENV=production
EXPOSE 3001
CMD ["node", "server/engine-bff.mjs"]
